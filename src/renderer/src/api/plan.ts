export const planListDayApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

export const planListDailyApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

export const planAddDailyApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

export const planAddDayApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

export const planDelApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

export const planUpdDayApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}
