

/**
 * 任务统计
 */
export const taskStatApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 事项统计
 */
export const todoStatApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

//#region ---------------------------------------- todo ----------------------------------------

/**
 * 事项列表
 */
export const todosApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 事项列表
 */
export const countTaskApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 新增阶段性事项
 */
export const addPhasedApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 修改阶段性事项的名称
 */
export const updTodoNameApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 完成阶段性事项
 */
export const openTodoApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 完成阶段性事项
 */
export const completedTodoApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 完成阶段性事项
 */
export const exportTodoApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

//#endregion

//#region ---------------------------------------- task ----------------------------------------
/**
 * 某天或某个事项的任务列表
 */
export const tasksApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 某天或某个事项的任务列表
 */
export const taskInfoApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 新增任务
 */
export const addTaskApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 修改任务的主要信息
 */
export const updTaskApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 移动到 waiting
 */
export const toWaitingApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 移动到 processing
 */
export const toProcessingApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 移动到 completed
 */
export const toCompletedApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 删除节点
 */
export const delTaskApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

/**
 * 标签集合
 */
export const taskTagsApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}

export const taskTransferApi = (_req?: object): Promise<R<any>> => {
  return new Promise(() => {})
}
//#endregion
